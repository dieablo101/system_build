#!/bin/sh

# if [ -d "./src_temp/systemSrc" ] ; then
    ## git pull origin main
    # echo "systemSrc folder exists, system is already installed"
# else
    # echo "'systemSrc folder non existant"

# fi